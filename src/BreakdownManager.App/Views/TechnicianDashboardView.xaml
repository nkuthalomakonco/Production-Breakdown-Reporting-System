<UserControl x:Class="BreakdownManager.App.Views.TechnicianDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="900" d:DesignHeight="650"
             x:Name="Root">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Technician Dashboard" Style="{StaticResource SectionHeader}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Working as:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding Technicians}" SelectedItem="{Binding CurrentTechnician}"
                      DisplayMemberPath="FullName" Padding="8" MinWidth="180"/>
            <Button Content="Refresh" Command="{Binding RefreshCommandCommand}" Margin="16,0,0,0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="16,0,0,0"
                       Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Unassigned jobs, open to anyone to accept -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Open Jobs" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding UnassignedJobs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDD" BorderThickness="1"
                                    CornerRadius="4" Padding="14" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Priority}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Machine.Name}" FontSize="15" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="Gray" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding ReportedAt, StringFormat='Reported {0:HH:mm}'}" Foreground="Gray" Margin="0,4,0,8"/>
                                    <Button Content="Accept"
                                            Command="{Binding DataContext.AcceptJobCommand, ElementName=Root}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- My jobs, progressing through the repair workflow -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="My Jobs" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding MyJobs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDD" BorderThickness="1"
                                    CornerRadius="4" Padding="14" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{Binding TicketNumber}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Machine.Name}" FontSize="15" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="Gray" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding Status, StringFormat='Status: {0}'}" Margin="0,8,0,8" FontWeight="SemiBold"/>
                                    <Button Content="Advance to Next Stage"
                                            Command="{Binding DataContext.AdvanceStatusCommand, ElementName=Root}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
